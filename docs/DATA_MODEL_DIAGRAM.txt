╔══════════════════════════════════════════════════════════════════════════════╗
║                        uteki.open 数据模型组织架构                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                          📂 Project Structure                                │
└─────────────────────────────────────────────────────────────────────────────┘

backend/uteki/
│
├── common/                        # 共享基础组件
│   ├── base.py                   ┌──────────────────────────────────┐
│   │   ├── Base                  │ SQLAlchemy DeclarativeBase       │
│   │   ├── TimestampMixin        │ - created_at, updated_at         │
│   │   └── UUIDMixin             │ - id (UUID)                      │
│   │                              └──────────────────────────────────┘
│   ├── database.py                # 数据库连接管理
│   └── config.py                  # 全局配置
│
└── domains/                       # DDD领域层
    │
    ├── admin/                    ┌──────────────────────────────────┐
    │   ├── models.py              │ Database Models (SQLAlchemy)    │
    │   ├── schemas.py             │ API Schemas (Pydantic)          │
    │   ├── repository.py          │ Data Access Layer               │
    │   ├── service.py             │ Business Logic Layer            │
    │   └── api.py                 │ API Routes (FastAPI)            │
    │                               └──────────────────────────────────┘
    ├── trading/
    ├── data/
    ├── agent/
    ├── evaluation/
    └── dashboard/


┌─────────────────────────────────────────────────────────────────────────────┐
│                     🎯 数据模型的三种类型                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│  1. Database Models          │
│  (models.py)                 │
├──────────────────────────────┤
│  - SQLAlchemy ORM            │
│  - 映射到数据库表              │
│  - 继承Base + Mixins         │
│  - 定义字段、索引、外键        │
└──────────────────────────────┘
         │
         ├─ 继承 ─→ Base (DeclarativeBase)
         ├─ 继承 ─→ UUIDMixin (id)
         └─ 继承 ─→ TimestampMixin (created_at, updated_at)


┌──────────────────────────────┐
│  2. API Schemas              │
│  (schemas.py)                │
├──────────────────────────────┤
│  - Pydantic BaseModel        │
│  - API请求/响应验证           │
│  - 不含敏感信息               │
│  - 支持继承组合               │
└──────────────────────────────┘
         │
         ├─ APIKeyBase (共享字段)
         ├─ APIKeyCreate (创建请求)
         ├─ APIKeyUpdate (更新请求)
         └─ APIKeyResponse (API响应)


┌──────────────────────────────┐
│  3. Domain Objects (可选)    │
│  (entities.py)               │
├──────────────────────────────┤
│  - 纯Python对象               │
│  - 业务逻辑封装               │
│  - 不依赖数据库               │
│  - 可用dataclass             │
└──────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                        📊 数据流转示例                                        │
└─────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
  API请求 → 数据库存储
═══════════════════════════════════════════════════════════════════════════════

    Client (HTTP POST)
         │
         │ JSON: {"provider": "openai", "api_key": "sk-xxx", ...}
         ▼
    ┌─────────────────────────┐
    │  1. API Layer           │
    │  (api.py)               │
    │  - FastAPI endpoint     │
    └─────────────────────────┘
         │
         │ Pydantic validates input
         ▼
    ┌─────────────────────────┐
    │  2. Schema Layer        │
    │  (schemas.py)           │
    │  - APIKeyCreate         │
    └─────────────────────────┘
         │
         │ Pass validated data
         ▼
    ┌─────────────────────────┐
    │  3. Service Layer       │
    │  (service.py)           │
    │  - Encrypt API key      │
    │  - Business logic       │
    └─────────────────────────┘
         │
         │ Create ORM object
         ▼
    ┌─────────────────────────┐
    │  4. Repository Layer    │
    │  (repository.py)        │
    │  - Data access          │
    └─────────────────────────┘
         │
         │ SQLAlchemy insert
         ▼
    ┌─────────────────────────┐
    │  5. Database Model      │
    │  (models.py)            │
    │  - APIKey (ORM)         │
    └─────────────────────────┘
         │
         │ SQL INSERT
         ▼
    ┌─────────────────────────┐
    │  PostgreSQL Database    │
    │  admin.api_keys         │
    └─────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
  数据库查询 → API响应
═══════════════════════════════════════════════════════════════════════════════

    PostgreSQL Database
         │
         │ SQL SELECT
         ▼
    ┌─────────────────────────┐
    │  1. Database Model      │
    │  (models.py)            │
    │  - APIKey (ORM)         │
    └─────────────────────────┘
         │
         │ SQLAlchemy query
         ▼
    ┌─────────────────────────┐
    │  2. Repository Layer    │
    │  (repository.py)        │
    │  - Query execution      │
    └─────────────────────────┘
         │
         │ Return ORM object
         ▼
    ┌─────────────────────────┐
    │  3. Service Layer       │
    │  (service.py)           │
    │  - Decrypt if needed    │
    │  - Format data          │
    └─────────────────────────┘
         │
         │ Convert to schema
         ▼
    ┌─────────────────────────┐
    │  4. Schema Layer        │
    │  (schemas.py)           │
    │  - APIKeyResponse       │
    └─────────────────────────┘
         │
         │ Pydantic serializes
         ▼
    ┌─────────────────────────┐
    │  5. API Layer           │
    │  (api.py)               │
    │  - FastAPI response     │
    └─────────────────────────┘
         │
         │ JSON: {"id": "...", "provider": "openai", ...}
         ▼
    Client (HTTP Response)


┌─────────────────────────────────────────────────────────────────────────────┐
│                     🏗️ 代码示例：完整的Model定义                              │
└─────────────────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════════════════╗
║  File: backend/uteki/domains/admin/models.py                              ║
╚═══════════════════════════════════════════════════════════════════════════╝

from uteki.common.base import Base, UUIDMixin, TimestampMixin
from sqlalchemy import String, Boolean, JSON, ForeignKey
from sqlalchemy.orm import Mapped, mapped_column

class LLMProvider(Base, UUIDMixin, TimestampMixin):
    """
    LLM提供商配置
    
    继承结构:
    - Base (SQLAlchemy DeclarativeBase)
    - UUIDMixin (id: str)
    - TimestampMixin (created_at, updated_at)
    """
    
    __tablename__ = "llm_providers"
    __table_args__ = {"schema": "admin"}
    
    # 业务字段
    provider: Mapped[str] = mapped_column(String(50), nullable=False)
    model: Mapped[str] = mapped_column(String(100), nullable=False)
    
    # 外键关联
    api_key_id: Mapped[str] = mapped_column(
        String(36),
        ForeignKey("admin.api_keys.id", ondelete="CASCADE"),
        nullable=False
    )
    
    # 配置字段
    model_config: Mapped[Optional[Dict]] = mapped_column(JSON, nullable=True)
    is_default: Mapped[bool] = mapped_column(Boolean, default=False)


╔═══════════════════════════════════════════════════════════════════════════╗
║  File: backend/uteki/domains/admin/schemas.py                             ║
╚═══════════════════════════════════════════════════════════════════════════╝

from pydantic import BaseModel, Field
from datetime import datetime
from typing import Optional, Dict, Any

# 1. Base Schema (共享字段)
class LLMProviderBase(BaseModel):
    provider: str = Field(..., description="提供商名称")
    model: str = Field(..., description="模型名称")
    display_name: str
    is_default: bool = False

# 2. Create Schema (API创建请求)
class LLMProviderCreate(LLMProviderBase):
    api_key_id: str = Field(..., description="关联的API密钥ID")
    model_config: Optional[Dict[str, Any]] = None

# 3. Update Schema (API更新请求)
class LLMProviderUpdate(BaseModel):
    display_name: Optional[str] = None
    is_default: Optional[bool] = None
    model_config: Optional[Dict[str, Any]] = None

# 4. Response Schema (API响应)
class LLMProviderResponse(LLMProviderBase):
    id: str
    api_key_id: str
    model_config: Optional[Dict[str, Any]]
    created_at: datetime
    updated_at: datetime
    
    class Config:
        from_attributes = True  # 允许从ORM对象创建


┌─────────────────────────────────────────────────────────────────────────────┐
│                   🔄 跨Domain数据共享                                         │
└─────────────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════════
  ❌ 错误方式：直接跨domain导入models
════════════════════════════════════════════════════════════════════════════════

    ┌──────────────────┐          ┌──────────────────┐
    │  Trading Domain  │  ─────→  │  Admin Domain    │
    │                  │          │                  │
    │  import models   │  ❌错误   │  models.py       │
    └──────────────────┘          └──────────────────┘
                                          │
                                          │ 违反DDD原则
                                          │ 创建紧耦合


════════════════════════════════════════════════════════════════════════════════
  ✅ 正确方式：通过Service层通信
════════════════════════════════════════════════════════════════════════════════

    ┌──────────────────┐          ┌──────────────────┐
    │  Trading Domain  │          │  Admin Domain    │
    │                  │          │                  │
    │  需要API密钥      │  ──→    │  service.py      │
    │                  │  请求    │                  │
    └──────────────────┘          └────────┬─────────┘
                                          │
                                          │ 查询
                                          ▼
                                  ┌──────────────────┐
                                  │  models.py       │
                                  │  APIKey          │
                                  └──────────────────┘

    代码示例:
    
    # Trading domain不直接导入admin models
    from uteki.domains.admin.service import api_key_service
    
    async def get_exchange_api_key():
        # 通过service层获取
        api_key = await api_key_service.get_api_key_by_provider(
            session, "okx", "production"
        )
        return api_key


┌─────────────────────────────────────────────────────────────────────────────┐
│                    📐 设计原则总结                                            │
└─────────────────────────────────────────────────────────────────────────────┘

1. 单一职责原则 (SRP)
   ├─ models.py:     只负责数据库映射
   ├─ schemas.py:    只负责API数据验证/序列化
   ├─ repository.py: 只负责数据访问
   ├─ service.py:    只负责业务逻辑
   └─ api.py:        只负责HTTP路由

2. Domain隔离原则
   ├─ 每个domain独立管理自己的models和schemas
   ├─ 跨domain通信通过service层
   └─ 避免直接访问其他domain的models

3. 不泄露敏感信息
   ├─ Schema层过滤敏感字段
   ├─ 只返回必要信息
   └─ 使用掩码或标志位

4. 使用Mixin复用
   ├─ UUIDMixin: 统一ID策略
   ├─ TimestampMixin: 统一时间戳
   └─ 避免重复代码


┌─────────────────────────────────────────────────────────────────────────────┐
│                  📊 数据库Schema组织                                          │
└─────────────────────────────────────────────────────────────────────────────┘

    PostgreSQL Database
    │
    ├── admin schema
    │   ├── api_keys
    │   ├── users
    │   ├── llm_providers
    │   ├── exchange_configs
    │   └── data_source_configs
    │
    ├── trading schema
    │   ├── orders
    │   ├── positions
    │   └── trades
    │
    ├── data schema
    │   ├── klines
    │   └── tickers
    │
    ├── agent schema
    │   ├── agent_tasks
    │   └── agent_executions
    │
    ├── evaluation schema
    │   ├── backtests
    │   └── metrics
    │
    └── dashboard schema
        └── widgets


    每个domain = 一个PostgreSQL schema
    避免表名冲突，清晰的组织结构


╔══════════════════════════════════════════════════════════════════════════════╗
║                               END OF DIAGRAM                                  ║
╚══════════════════════════════════════════════════════════════════════════════╝
