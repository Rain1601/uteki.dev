# No Spec Changes

This change is a pure UI/CSS fix (scrollbar, layout, styling). No functional requirements are added, modified, or removed. No spec-level behavior changes.

See `proposal.md` and `design.md` for details.
